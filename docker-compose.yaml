services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_DB: carcontrol_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 02040638
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
    driver: local